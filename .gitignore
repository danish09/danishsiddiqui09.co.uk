*.js
!jest.config.js
*.d.ts
node_modules

# CDK asset staging directory
.cdk.staging
cdk.out

# CDK context cache — holds looked-up hosted zone IDs (account-specific)
cdk.context.json

# Personal documents (CV etc.) — repo is public, never commit these
docs/
